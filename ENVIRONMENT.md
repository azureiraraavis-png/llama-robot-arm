# 환경 이식 — 무엇이 옮겨지고 무엇이 안 옮겨지는가

## 결론

**Docker도 exe도 이 프로젝트에는 맞지 않습니다.** 대신 **버전 고정 + 가상환경 + 점검표**로 갑니다.

```
원래 컴퓨터에서    py -3.12 freeze_env.py      ← 지금 판을 그대로 받아 적기
새 컴퓨터에서      py -3.12 setup_env.py       ← 그 판대로 세우기
                   py -3.12 checkup.py --deep  ← 온전한지 확인
```

---

## 왜 Docker가 아닌가

컨테이너의 목적은 **호스트로부터의 격리**입니다. 그런데 이 프로그램이 하는 일의 전부가
**호스트의 하드웨어를 만지는 것**입니다. 목적이 정면으로 충돌합니다.

| 필요한 것 | 윈도우 컨테이너에서 |
|---|---|
| COM 포트 (아두이노) | 직접 불가. `usbipd-win`으로 USB/IP 터널을 따로 세워야 함 |
| 웹캠 (손 추적·가리키기) | 직접 불가 |
| 마이크 (음성) | 직접 불가 |
| GPU | WSL2 + NVIDIA Container Toolkit으로 가능하지만 층이 하나 더 |
| 창 띄우기 (`cv2.imshow`) | 윈도우에 X 서버를 따로 띄워야 함 |

**Docker가 맞는 자리도 있습니다 — 재학습입니다.** 학습은 하드웨어를 안 만지고
GPU와 데이터만 있으면 되므로, 리눅스 서버에서 컨테이너로 돌리는 것이 오히려 낫습니다.
필요해지면 그때 `train` 묶음만 담은 이미지를 만들면 됩니다.

## 왜 exe가 아닌가

**우리가 오늘 하루를 태운 그 벽에 그대로 부딪힙니다.** PyInstaller가 만든 서명 없는 exe는
윈도우 11의 스마트 앱 제어가 막습니다. PyInstaller 저장소에 이 문제로 열린 이슈가 있고,
백신 오탐도 고질적입니다. 코드 서명 인증서는 연 단위로 돈이 듭니다.

크기도 문제입니다.

| 대상 | exe 크기 | 현실성 |
|---|---|---|
| `llm_arm_bridge` (팔 조종) | 약 20MB | 가능 |
| `hand_track` (mediapipe) | 300MB+ | 모델 파일을 따로 넣어야 함 |
| `voice_arm` (torch + transformers) | 2GB 이상 | 사실상 불가 |

그리고 exe를 만들어도 **Ollama와 라마 모델은 따로 설치해야 합니다.** 가장 무거운 의존성이
exe 밖에 남습니다. 포장의 이득이 거의 없습니다.

---

## 진짜 문제 — 이 환경은 원래 완전히 재현할 수 없습니다

의존성 목록에 `point_calib.json`이 있습니다. 그 안의 숫자는
**이 팔 한 개체를 실제로 재서 나온 값**입니다. 다른 팔에 그대로 넣으면 틀립니다.

| 옮겨지는 것 | 방법 |
|---|---|
| 파이썬 패키지 | `requirements-*.txt` (freeze_env.py가 만듦) |
| 코드·펌웨어·데이터셋 | git |
| 학습된 어댑터 | Hugging Face |
| **옮겨지지 않는 것** | |
| 파이썬 3.12 자체 | 손으로 설치 |
| NVIDIA 드라이버 | 손으로 설치 |
| Ollama와 `llama3.1:8b` (약 5GB) | `ollama pull` |
| 아두이노에 구워진 펌웨어 | 아두이노 IDE로 다시 굽기 |
| **보정값 (`*_calib.json`)** | **새 팔이면 다시 재야 합니다** |
| 로봇팔 그 자체 | — |

그래서 목표를 **"한 번에 재현"이 아니라 "빠뜨림 없는 재설치"**로 잡았습니다.
`checkup.py --deep`이 그 점검표입니다.

---

## 세 가지 도구

### `freeze_env.py` — 원래 컴퓨터에서 한 번

지금 잘 도는 컴퓨터의 판을 **읽어서** 적습니다. 제 기억이나 문서에서 가져오지 않습니다.

`pip freeze`를 그냥 쓰지 않은 이유는, 그 컴퓨터에 이 프로젝트와 무관한 패키지도 많기 때문입니다.
전부 적어 두면 다음 컴퓨터에서 쓸데없는 것을 깔다가 엉뚱한 데서 막힙니다.
**우리가 실제로 import 하는 것만** 골라 적습니다.

`torch`는 특별히 다룹니다. `2.13.0+cu126`처럼 CUDA 판임을 나타내는 꼬리표를 읽어서
`--extra-index-url`을 요구사항 파일 안에 같이 적어 둡니다. 이걸 놓치면 다음 컴퓨터에서
**CPU 판이 깔려 조용히 느려집니다.** 에러가 안 나서 알아채기 어렵습니다.

### `setup_env.py` — 새 컴퓨터에서

파이썬 판을 확인하고, `.venv`를 만들고, 묶음별로 설치한 뒤 `checkup.py`로 확인합니다.

**PowerShell이 아니라 파이썬으로 쓴 이유** — `.ps1`은 실행 정책에 막힙니다. 실제로 겪었습니다.
파이썬은 어차피 깔아야 하니 설치 도구도 파이썬이 확실합니다.

**venv를 쓰는 이유** — `python`과 `py -3.12`가 서로 다른 파이썬을 가리켜서
"torch가 없다"는 엉뚱한 오류를 겪었습니다. venv를 쓰면 폴더 안의 `.venv` 하나만 보면 됩니다.

묶음이 넷입니다. **필요한 것만 깔 수 있는 것이 요점입니다.**

| 묶음 | 내용 | 무게 |
|---|---|---|
| `core` | 팔 조종 · 춤 · 아두이노 통신 | 몇 MB |
| `vision` | 카메라 · 가리키기 · 손 추적 | 약 100MB |
| `voice` | 음성 인식 (torch) | **2GB 이상** |
| `train` | 재학습 | 큼 |

발표만 할 거라면 `--tier core` 하나로 충분합니다.

### `checkup.py --deep` — 확인

이미 있는 것입니다. 기능별로 무엇이 빠졌는지, **빠진 것이 어느 기능을 못 쓰게 만드는지**를 말합니다.

---

## 새 컴퓨터 설치 순서

```powershell
# 1. 파이썬 3.12 설치 (python.org)
# 2. 저장소 받기
git clone https://github.com/azureiraraavis-png/llama-robot-arm
cd llama-robot-arm

# 3. 환경 세우기 — 발표만 할 거면 core 하나로 충분
py -3.12 setup_env.py --tier core
#    전부 다:  py -3.12 setup_env.py --all

# 4. Ollama 설치 후
ollama pull llama3.1:8b

# 5. 아두이노 IDE로 arduino/robot_arm_llm 굽기

# 6. 확인
.venv\Scripts\python checkup.py --deep
```

**팔이 다른 개체라면 보정을 다시 해야 합니다.**
그리퍼가 닫히는 각도부터 재고(`test_arm.py`), 가리키기 보정을 다시 찍으세요(`calibrate_point.py`).
`*_calib.json`을 그대로 쓰면 팔이 엉뚱한 곳을 가리킵니다.

---

## 나중에 필요해지면

- **다른 사람에게 배포**해야 한다면 → exe보다 **저장소 + 이 설치 순서**를 주는 편이 확실합니다.
  스마트 앱 제어에 안 막히고, 무엇이 깔리는지 눈에 보입니다.
- **여러 대에 반복 설치**해야 한다면 → `setup_env.py`를 그대로 쓰면 됩니다. 그게 이 도구의 목적입니다.
- **재학습을 서버에서** 돌린다면 → 그때는 Docker가 맞습니다. `train` 묶음만 담고
  하드웨어는 아무것도 붙이지 않으면 됩니다.
- **정말 exe가 필요하다면** → `llm_arm_bridge.py`(core 묶음)만 대상으로 하세요. 20MB쯤 나옵니다.
  다만 서명 없이는 스마트 앱 제어가 켜진 컴퓨터에서 막힙니다.
