# 라마와 로봇팔 — 기록

**Adeept 5-DOF 로봇팔 (Arduino UNO) + Llama 3.1 8B + RTX 4070**

목표: *"오픈소스 대형언어모델 라마를 이용(학습 및 시험)해서 아두이노 로봇팔을 원하는 대로 자율 작동시킨다."*

- **첫째 날** — 조립. 나사와 서보 혼과 씨름하다 하루가 갔다
- **둘째 날** — 배선 확정부터 전용 모델 파인튜닝까지
- **며칠 뒤** — 학습된 모델의 결함 추적, 카메라로 눈 달기, 그리고 춤
- **또 며칠 뒤** — 손 따라 하기와 목소리를 한나절에, 그리고 작업공간 이사

*하드웨어 검증(배선 실측, 그리퍼 보정)은 첫째 날과 둘째 날에 걸쳐 있었다.*

---

## 0. 설계 — 처음에 정한 두 가지 원칙

**LLM은 아두이노 위에서 돌지 않는다.** 아두이노는 메모리가 KB 단위다. 그래서 역할을 나눴다.

```
[자연어 지시]
   ↓
[PC] Llama = 자연어 → 명령 시퀀스 번역기·계획기
   ↓
[PC] 파이썬 브리지 = 검증 · 산수 · 시계 · 반복 · 의도 강제
   ↓ USB 시리얼
[아두이노] = 각도 한계 강제 · 서보 구동
```

**LLM의 출력은 절대 그대로 믿지 않는다.** 파이썬과 아두이노 양쪽에서 관절 한계를 강제한다.

이 두 원칙이 이후 모든 문제의 해법을 결정했다. 문제가 생길 때마다 답은 늘 같은 모양이었다 — **모델에게 무리한 일을 시키지 말고, 모델이 잘하는 언어 판단만 맡기고 나머지는 결정론적 코드가 한다.**

---

# 첫째 날 — 조립

하루의 대부분이 나사와 서보 혼이었다. 이 키트는 조립 자체가 만만치 않고,
특히 **서보를 팔에 고정하기 전에 90도로 맞춰두는 것**을 놓치면 뒤가 전부 뒤틀린다.
소프트웨어는 다음 날로 넘겼다.

---

# 둘째 날 — 배선 확정부터 파인튜닝까지

## 1. 하드웨어 검증 — "실물은 문서와 다르다"

*(조립 직후부터 이어진 작업이라 첫째 날과 둘째 날에 걸쳐 있다)*

조립을 마치고 펌웨어를 올렸는데 팔이 엉뚱하게 움직였다. 배선을 확정하는 데 세 번의 시도가 필요했다.

| 시도 | 가정한 배선 | 결과 |
|---|---|---|
| 1차 | 보드 실크 순서대로 D9, D6, D5, D3, D11 | 손목 대신 그리퍼가 여닫힘 |
| 2차 | 손목·그리퍼를 바꿔 D9, D6, D3, D10, D11 | 손목 자리가 무반응 |
| 3차 | **`pin_finder.ino`로 실측** | D9, D6, D5, D3, D11 (D10은 빈 포트) — 확정 |

추측을 반복하는 대신 **후보 핀 6개를 전부 열어두고 하나씩 눌러보는 진단 스케치**를 만든 것이 전환점이었다. 이 방식은 이후 데이터에서도, 카메라에서도 그대로 반복된다.

그 밖의 실측 보정:

- **그리퍼**: 기본값 105도로는 안 다물어짐 → 실측 **120도**에서 완전 닫힘, 안전 상한 125도
- **손목**: 위아래 까딱이는 축이 아니라 **좌우로 비트는 회전축(롤)**이었다
- 겪은 잔가시들: COM 포트 번호 불일치, `Missing FQBN`, 포트 점유 충돌

## 2. LLM 길들이기 — 발견의 연속

### 2-1. 단발 명령 문제 → 출력 형식 재설계

"천천히 앞으로 숙여서 인사해"에 모델이 `{"cmd":"speed","level":1}` 하나만 내놓고 끝냈다. 프롬프트에 규칙과 예시를 아무리 넣어도 고쳐지지 않았다.

진짜 원인은 **Ollama의 JSON 강제 모드가 "객체 하나를 만들고 끝내려는" 성향을 강화**한다는 것. 출력 형식을 배열 `[...]`에서 **`{"commands":[...]}` 객체**로 바꾸자 즉시 해결됐다.

> 모델이 규칙을 못 지킬 때, 규칙을 더 크게 외치는 것보다 **형식 자체를 모델의 성향에 맞추는 것**이 효과적일 때가 있다.

### 2-2. 인컨텍스트 학습을 눈으로 보다

*"한 마디씩 분리해서 학습시킨 후, 다음에 바로 두 마디를 붙이면 제대로 수행하는군요."*

브리지가 최근 대화 3건을 함께 보내기 때문이었다. 모델은 자기가 방금 만든 정답을 참고서 삼아 이어붙이기만 하면 됐다. 가중치는 그대로인데 프롬프트에 들어온 예시만으로 즉석에서 배우는 것 — 교과서에 나오는 인컨텍스트 학습을 로봇팔로 직접 시연한 셈이다.

**파인튜닝은 이 임시 기억을 가중치에 새겨 영구화하는 일**이라는 것이 여기서 분명해졌다.

### 2-3. 어순 민감성 — 최소 대립쌍

| 문장 | 결과 |
|---|---|
| 집게를 닫았다 여는 것을 **2번 반복**하시오 | ✅ |
| 집게를 **2번** 닫았다 열으시오 | ❌ |
| 집게를 **2번** 닫았다 **2번** 열으시오 | ✅ |

> 소형 모델에게는 **우아한 문장보다 중복이라도 명시적인 문장이 이긴다.**

### 2-4. 반복 페널티 — 기계적으로 구현된 반항기

*"왜 3번까지 잘 되다가 4번에서부터 안 되는 건지... 반항기가 온 걸까요?"*

언어 모델에는 방금 한 말을 또 하지 않으려는 억제 장치(repeat penalty, 기본 1.1)가 켜져 있다. 같은 지시를 세 번 성공시키면 눈앞에 똑같은 답이 세 개 쌓이고, 네 번째에 페널티가 억누른다. **문자 그대로 "또 그 말이야?"라고 반항한 것.** `repeat_penalty: 1.0`으로 껐고, `num_ctx`도 8192로 늘렸다.

### 2-5. 시간 감각과 세기 — 모델에게 없는 것

- **"5초 동안 도리도리 해"** → `wait 5000`만 내놓고 가만히 있었다. 서보 한 왕복이 몇 초인지 알 방법이 없다. **몸이 없으니 물리적 시간 감각이 없다.**
- **"5번 딱딱거려"** → 4번, 2번이 나왔다. 생성 중에 세면서 정확히 멈추는 것은 못 한다.

처방은 같았다. **모델이 말할 수 있는 명령을 만들어주고, 계산은 파이썬이 한다.**

```json
{"cmd":"repeat","seconds":5,"commands":[...]}   → 브리지가 실제 시계를 보며 반복
{"cmd":"repeat","times":5,"commands":[...]}     → 브리지가 정확히 5번 펼침
```

### 2-6. 상대 각도 — 모르는 건 묻게 한다

"뒤로 30도 기울여"에 15도든 30도든 늘 비슷한 값이 나왔다. 현재 각도를 알 수 없고 덧셈도 약하기 때문. `move` 명령을 추가해, 모델은 `{"cmd":"move","joint":1,"delta":30}`이라 **말만** 하고 브리지가 아두이노에게 `GET`으로 물어본 뒤 계산하게 했다.

```
↔ 관절 1: 현재 70도 +30 → 100도
```

## 3. 데이터 수집과 정제 — "데이터는 의도와 다르다"

브리지에 자동 기록 기능을 넣고 팔을 갖고 놀았다. **131건**이 모였다. 그런데 그대로 학습시켰다면 모델이 더 나빠졌을 것이다.

| 발견된 문제 | 예 |
|---|---|
| **직접 모순 6쌍** | "뒤로 15도"가 어떤 줄은 절대각 105, 어떤 줄은 상대이동 +15 |
| **방향 반전 5건** | "앞으로"가 165도(실제로는 뒤로)로 뒤집혀 기록됨 |
| **형식 불일치** | "n도씩"이 19건은 절대각, 1건만 상대이동 |
| **횟수 오류 2건** | "2번"인데 열기가 1회 |
| **home 단독 과대표집** | 13건(10%) — "뭘 시키든 일단 home" 편향 위험 |

정제해 **98건**으로 줄였다.

## 4. 파인튜닝 — 환경과의 싸움

RTX 4070(12GB)에서 QLoRA. 80억 파라미터 중 **4,194만 개(0.52%)**만 학습.

핵심 설계: **학습용 시스템 프롬프트를 일부러 짧게.** 규칙 9개와 예시 15개를 빼고 명령어 목록만 남겼다. 프롬프트에 있던 지식을 가중치로 옮기는 것이 목적이므로.

| 증상 | 원인 |
|---|---|
| `No matching distribution found for torch` | **Python 3.14**에 PyTorch CUDA 휠 없음 → 3.12 병행 설치 |
| `unexpected keyword argument 'warmup_ratio'` | transformers 5.x에서 인자 제거 → **시그니처를 읽어 맞추는** 호환 계층 |
| `'tokenizers.Encoding' object cannot be interpreted as an integer` | 5.x 반환형 변경 → 템플릿은 문자열로, 토큰화는 따로 |

### 성적표 (보류 문제 15건, temperature 0 고정)

| 기준 | 학습 전 | 학습 후 | 변화 |
|---|---|---|---|
| `valid` (실행 가능한 형태인가) | 100% | 100% | ― |
| `exact` (정답과 완전 일치) | 7% | **27%** | ▲ 20%p |
| `direction` (앞/뒤 규약) | 33% | **100%** | ▲ 67%p |
| `relative` (`move`를 쓰는가) | 0% | **100%** | ▲ 100%p |
| `sequence` (단발로 때우지 않는가) | 100% | 100% | ― |
| `count` (횟수) | 100% | 0% | *문제 1개 — 판단 불가* |

`move` 명령과 앞/뒤 방향 규약은 그날 만들어낸 개념이라 원래 Llama가 알 리 없었다. **프롬프트 없이도 나온다는 것은 가중치에 새겨졌다는 뜻이다.**

### 그날의 마지막 함정 — 내가 만든 모순

첫 학습 후 "-15도씩 굽히시오"가 이상했다. 데이터를 뒤져보니 정제 스크립트가 남긴 모순:

```
모든 관절을 -45도씩 접고...    → delta +45
모든 관절을 -30도씩 굽히시오   → delta +30
모든 관절을 -15도씩 굽히시오   → delta -15   ← 혼자 다름
```

규약을 확인했다 — *"뒤를 향해 굽혀야 합니다"* (마이너스 = 반대 방향). 그리고 배선 때 `pin_finder.ino`를 만들었듯, **`check_dataset.py`(데이터 검사기)**를 만들어 그 3건을 잡아내고 재학습했다.

시스템 프롬프트에 규칙도 예시도 없는 상태로 확인 순서를 전부 통과. 둘째 날은 여기서 끝났다.

---

# 며칠 뒤 — 결함 추적, 눈 달기, 그리고 춤

## 5. 학습된 모델의 결함 — 그리고 또 나였다

`arm_tuned.py`로 놀다가 두 증상이 잡혔다.

```
명령> 관절들을 움직여 뒤를 돌아보고 그대로 10초 동안 멈추시오
[계획] [... {"cmd":"wait","ms":4000}, {"cmd":"home"}]
```

**10초라고 했는데 4초**, 그리고 **"그대로 멈추시오"인데 끝에 `home`**. 유지하라는 지시를 명령이 정면으로 뒤집고 있었다.

데이터를 뒤져 원인을 규명했다. 전부 **1차 정제 스크립트의 결함**이었다.

### 서로 반대 방향인 버그 한 쌍

"`move` 명령이 있으면 끝의 `home`을 뗀다"는 규칙이 과했다.

```
모든 관절을 30도씩 굽히고, 5초 동안 가만히 있다가 원래 상태로 돌아가시오  → home 없음 ✗
모든 관절을 30도씩 굽히고, 10초 동안 가만히 있다가 원상복귀 하시오        → home 없음 ✗
```

**"돌아가시오"라고 명시했는데 안 돌아가도록** 가르친 것. 그리고 정반대로, 유지 판별 정규식에 "가만히"와 "~한 채"가 빠져서 이런 것들이 남았다.

```
팔을 앞으로 기울인 채 7초를 기다리시오   → home 없음
팔을 뒤로 기울인 채 7초를 기다리시오     → home 있음   ← 같은 패턴, 다른 답
```

같은 형태 9건 중 7:2로 갈렸다. 모델이 헷갈릴 수밖에.

### "모든 관절"의 정의가 흔들림

45도씩 접기 계열 5건은 집게(관절4)를 포함하고 4건은 제외. 30도씩 굽히기는 전부 제외.

### 시간 산수는 데이터 탓이 아니었다

초→밀리초 환산은 데이터상 **100% 일치**했다. 그런데도 10초에 4000을 냈다. **외운 문장에서는 맞히지만 새 문장에서 곱셈을 일반화하지 못하는 것.** 83건으로는 "초 × 1000"이 안 잡힌다.

## 6. 처방 — 원칙을 한 번 더 적용

### 곱셈 자체를 없앤다

```json
{"cmd":"wait","seconds":10}    ← 모델은 숫자를 옮겨 적기만, 환산은 브리지가
```

그리고 지금 당장(재학습 없이) 듣게 하려고 **`enforce_duration`**을 넣었다. 지시문의 "n초"와 계획의 대기가 어긋나면 지시문 쪽으로 맞춘다. 자리 수가 대응되지 않아 애매하면 손대지 않는다.

### 의도를 시스템이 지킨다

각도 한계를 아두이노가 강제하듯, **`enforce_home_policy`**가 복귀/유지 의도를 지킨다. 규약은 사이안 님이 정했다 — *"시간이 명시되면 대기 후 복귀."*

| 지시 | 결과 |
|---|---|
| 원상복구 하지 마 | 유지 |
| ~하고 원상복구 하시오 | 복귀 |
| 유지/멈춰/계속 | 유지 (시간이 있어도) |
| n초 + 기다려/대기/가만히 있어 | 그 시간 후 복귀 |
| 시간 없는 "기다려", "그 상태로" | 무기한 유지 |
| 판단 근거 없음 | 모델 판단 존중 |

첫 구현에는 빈틈이 있었다. "그대로"는 막았는데 **"그 상태로", "그 자세로"가 빠져** 새어나갔고, 시간 없는 "가만히 있으시오"도 통과했다. 표현 목록을 넓혀 다시 짰다.

### 규칙을 한 곳으로

판정 규칙이 브리지와 검사기 두 곳에 복사돼 있었다. **그것이 바로 오늘 잡은 사고의 씨앗과 같은 구조**다. 이제 `llm_arm_bridge.home_policy()` 하나만 규약을 정의하고 검사기는 그걸 가져다 쓴다.

### 데이터와 검사기 갱신

`arm_dataset_v3.jsonl` — 11곳 교정(복귀 2건 추가, 유지 계열 통일, 집게 제외 5건) + 긴 대기를 전부 `seconds` 표기로. `check_dataset.py` v2 — 기존 검사(모순·부호·방향·형식)에 **home 정책·시간 환산·"모든 관절" 대상**을 더했다. 교정 전 데이터에서 정확히 11건을 잡고, v3에서 0건.

### 고칠 수 없는 것

*"뒤를 돌아보시오"* — 베이스가 0~180도라 **물리적으로 뒤를 볼 수 없다.** 최대가 좌우 끝. 결론은 *"그냥 오른쪽이나 왼쪽으로 끝까지 돌게 하는 것이 최선"*.

## 7. 카메라 — 눈을 달기 시작하다

### 설계 판단 세 가지

**카메라는 측정 장치다.** 위치를 재는 건 기하학이지 언어가 아니므로 OpenCV가 맡는다. 비전 LLM(Llama 3.2 Vision)은 한 장에 몇 초씩 걸려 추적에 못 쓰고, 8B와 같이 올리면 12GB로 빠듯하다.

**역기구학을 쓰지 않는다.** 레이저 커팅 부품에 마이크로 서보라 링크 길이·서보 영점·기어 유격이 전부 부정확하다. 계산으로 뽑은 각도는 실제와 어긋난다. 그리퍼 보정 때처럼 **실측으로 가르친다.**

**그래서 카메라 위치를 몰라도 된다.** 픽셀→각도 대응을 직접 배우므로 카메라의 위치·각도·렌즈가 변환식에 들어가지 않는다. 조건은 하나 — 보정 후 카메라가 움직이지 않을 것.

### 1단계: 물체를 안정적으로 잡기

`vision.py`(순수 시각 처리) + `vision_check.py`(색 범위 맞추기 도구). 화면에서 물체를 클릭하면 그 색으로 범위가 자동 설정되고, **흔들림 수치**가 실시간으로 표시된다(10px 미만이 목표).

여기서도 환경이 세 번 걸었다.

| 증상 | 원인 |
|---|---|
| `No such file or directory` | 파일을 다운로드 폴더에 저장하지 않음 |
| 카메라 "없음" | 열린 직후 첫 프레임 읽기 실패를 내가 실패로 판정 → 워밍업 재시도 추가 |
| `열림 / 읽기 실패` | **윈도우 카메라 앱이 이미 점유 중** |

마지막 것은 둘째 날 COM 포트 충돌과 똑같은 문제였다 — 장치는 한 프로그램만 잡는다. 진단을 위해 번호×백엔드를 전부 시험하는 `--diag`와, 해상도·포맷 조합을 훑는 `--probe`를 만들었다. OpenCV **5.0.0**이 깔려 있어서 `findContours` 반환 개수와 백엔드 상수도 미리 방어했다.

### 색 선정에서 배운 것

첫 시도는 주황 계열(H 4~28, S min 30)이었는데 **팔 자체가 나무색이라 같은 대역**이었다. 마스크에 팔이 하얗게 잡히면 검출 중심이 물체가 아니라 팔로 튄다. 최종 선택은 **파란 여행용 티슈 팩**(H 89~113, S min 125) — 나무 팔과 색상환에서 정반대라 깔끔하게 분리된다.

> 배경과 대비되는 색을 일부러 고르는 것은 실제 로봇 실험의 표준 관행이다.

### 2단계: 집기 보정 — 실패, 그리고 그 이유

`calibrate_arm.py`를 만들 때 이미 문제 하나를 알고 있었다. **팔이 물체 위로 가면 팔이 물체를 가린다.** 그래서 한 지점을 두 박자로 기록하게 했다 — 팔을 치우고 위치를 찍고 `[r]`, 팔을 몰고 가서 각도를 찍는다 `[스페이스]`.

그런데 6점을 모아 열어보니 물리적으로 불가능한 데이터였다.

```
[525, 19] → 베이스 90도 (정면)
[514, 26] → 베이스 20도 (오른쪽)
```

화면상 **13픽셀 떨어진 두 지점인데 팔이 70도 다른 방향**을 향한다. 같은 자리의 물체를 정면에서도 오른쪽에서도 집을 수는 없다. 이런 쌍이 3개, 픽셀 x와 베이스의 상관계수는 **+0.25**(쓸 만하려면 ±0.9 이상). 커버리지는 화면의 **7.7%**, 9칸 중 2칸.

원인은 물어보고서야 알았다 — **티슈를 손에 들고 계셨다.** 평평한 팩이 바닥에 누워 있으면 이 그리퍼로는 집을 수가 없어서였다. 그런데 이 방식의 전제가 "물체는 한 자리에 가만히 있고 팔에게 그 자리를 가르친다"이므로, 물체가 매번 움직이면 대응 자체가 성립하지 않는다.

> 또 짐작으로 진단하다 헛다리를 짚었다. 로그는 "무엇이 잘못됐는가"는 말해주지만
> "카메라 앞에서 무슨 일이 있었는가"는 말해주지 않는다.

도구도 고쳤다. **기록 즉시 모순 검사**(가까운 픽셀인데 각도가 크게 다르면 거부), 3×3 격자 커버리지 표시, 기록 시점 화면 저장, 저장 시 품질 요약. 실패한 그 데이터를 다시 넣어보면 문제의 점이 정확히 걸린다.

### 목표를 낮추다 — 집기에서 가리키기로

하드웨어의 한계가 분명해졌다. 평평한 티슈는 이 그리퍼로 못 집고, 팔은 레이저 커팅 나무에 마이크로 서보라 카메라를 얹을 수도 없다. 그래서 **집기를 접고 가리키기로 목표를 낮췄다.**

이 판단이 문제를 통째로 단순하게 만들었다.

| | 집기 | 가리키기 |
|---|---|---|
| 물체 | 집을 수 있어야 함 | **누워 있어도 됨** |
| 팔 | 물체에 닿아야 함 | **닿을 필요 없음** (제자리에서 방향만) |
| 대응 | 화면 2차원 → 관절 여러 개 | **화면 x → 베이스 하나** |
| 검증 | 어려움 | **단조성** — 어긋나면 즉시 안다 |
| 부담 | 그리퍼·무게중심에 힘 | 베이스만 회전 |

좁은 책상도 문제였다. 그래서 보간 방식을 바꿨다 — 보정점 사이는 보간하되 **바깥은 직선으로 연장 추정**한다. 3점만 찍어도 화면 전체를 덮을 수 있게.

### 결과

5점을 찍었다. 직선 맞춤에서 **최대 오차 1.9도.**

```
x=378  실제 40도 / 직선 39.1도   +0.9
x=426  실제 30도 / 직선 31.0도   -1.0
x=483  실제 20도 / 직선 21.3도   -1.3
x=531  실제 15도 / 직선 13.1도   +1.9
x=576  실제  5도 / 직선  5.5도   -0.5
```

서보 유격을 감안하면 사실상 완벽한 대응이다. 단조 관계도 깨끗하다.
`point_at.py`로 티슈를 책상 위에서 좌우로 움직이면 **팔이 따라 돌아본다.**

떨림을 막기 위해 세 가지를 두었다 — 3도 미만 변화는 무시(불감대), 최근 5프레임 중앙값(한 프레임 튐 무시), 0.35초 최소 간격.

*"생각보다 잘 따라 움직입니다."*

---

## 8. 춤 — 같은 원칙, 세 번째

카메라를 접고 나서 "이제 춤 추게 만듭시다"로 방향을 틀었다. 그리고 여기서도 답은 같았다.

### 왜 전체 자세(P) 명령인가

지금까지는 관절을 하나씩 `S`로 움직였다. 춤에는 맞지 않는다 — 동작이 뚝뚝 끊긴다.
펌웨어의 `moveTo`는 **모든 관절을 동시에** 목표로 몰아가므로, `P`(전체 자세)로 보내면 흐른다.
안무는 결국 "자세의 나열"이고, 그 사이는 펌웨어가 이어준다.

### 박자에 맞추려고 속도를 역산했다

펌웨어는 1도당 `(22 - speed*2)`ms로 움직인다. 그러면 한 박자에 D도를 움직여야 할 때
필요한 속도를 거꾸로 구할 수 있다.

```
ms_per_deg = beat_ms / D
speed = (22 - ms_per_deg) / 2
```

큰 동작도 작은 동작도 같은 박자에 떨어지고, 급가속이 생기지 않는다. 계산이 박자보다 빨리
끝나면 남는 시간만큼 `WAIT`로 채운다. 이것도 **모델이 아니라 코드가 하는 산수**다.

그 위에 안전장치를 두 겹 얹었다. 하드웨어 한계보다 안쪽으로 잡은 **춤 전용 범위**(어깨·팔꿈치 50~130도)와,
**한 박자에 65도 이상 움직이지 않기**(넘으면 자동으로 두 박자로 나눈다).

### 그리퍼 선이 빠졌다

*"아이고! 자꾸 집게 쪽 서보 코드가 빠지네요!"*

그리퍼 서보는 팔 맨 끝에 있어서 선이 팔 전체를 따라 내려온다. **손목이 회전하면 그 선이 비틀린다.**
안무에 손목을 크게 돌리는 동작이 많았던 것이 원인이었다.

처방은 물리와 소프트웨어 양쪽. 선에 여유를 주고 팔을 따라 고정하는 것과, **손목 회전 폭을 70도에서 48도로**
줄이는 것. 여기에 안전 손잡이 두 개를 달았다 — `--amp`(모든 동작을 홈 쪽으로 당겨 작게)와
`--no-grip`(집게 동작 건너뛰기).

> 소프트웨어가 시킬 수 있는 것과 하드웨어가 버틸 수 있는 것은 다르고,
> 그 차이는 **실제로 돌려봐야만** 드러난다. 그리퍼 120도도, 팔이 물체를 가리는 것도,
> 티슈를 못 집는 것도 그랬다. 이번엔 선이 알려줬다.

### 자연어에 합치기

여기서도 원칙 그대로였다. **모델은 "어떤 춤을 출지"만 말하고, 안무와 박자는 코드가 갖는다.**

```json
{"cmd":"dance","routine":"wave"}     ← 모델이 내는 것은 이게 전부
{"cmd":"dance","routine":"all","bpm":150,"amp":0.6,"grip":false}
```

`repeat`의 횟수 세기, `wait`의 초 환산과 완전히 같은 구조다. 브리지가 이 한 줄을
수십 줄의 자세 시퀀스로 펼친다. 한국어 이름("웨이브", "도리도리", "로봇")도 알아듣게 별칭을 뒀다.

가는 길에 `404` 하나를 만났다 — Ollama는 켜져 있는데 모델을 못 찾는 상태였다(`llama3.1:8b`가 지워져 있었다).
매 명령마다 같은 오류만 보이길래, **시작할 때 모델 유무를 확인하고 설치된 목록을 보여주도록** 고쳤다.

### 결과

```
명령> 춤 춰봐
명령> 웨이브 한번 해봐
명령> 살살 춤춰, 집게는 빼고
명령> 인사하고 나서, 30초 동안 로봇춤을 추시오
```

마지막 문장이 특히 반갑다. **동작 합성("~하고 나서")과 시간 지정("30초 동안")과 안무 선택이
한 문장 안에 섞여 있는데** 그걸 해낸다. 둘째 날에 "두 마디만 붙여도 흔들린다"고 적었던 그 모델이다.

---

# 또 며칠 뒤 — 손 따라 하기와 목소리, 한나절에

## 9. 손을 따라 하기 — 그리고 사라진 API

"외부 카메라로 사람 손 하나를 인식하고, 기계팔이 비슷하게 움직이게" 가 새 목표였다.

### 인터넷 예제가 전부 틀린 이유

`mediapipe`를 깔고 첫 줄을 쓰기 전에 확인부터 했다. 그리고 발견했다 —
**`mp.solutions.hands`가 0.10.30부터 완전히 사라졌다.** 검색해서 나오는 예제의 열에 아홉이
그 방식이다. 그대로 따라 썼다면 한참을 헤맸을 것이다.

지금은 Tasks API(`vision.HandLandmarker`) + 별도 모델 파일(`hand_landmarker.task`)을 쓴다.

### 손의 무엇을 팔의 무엇에 대응시킬 것인가

| 손 | → | 팔 |
|---|---|---|
| 좌우 위치 | → | 베이스 회전 |
| 상하 위치 | → | 어깨 |
| **손의 크기** (= 카메라와의 거리) | → | 팔꿈치 |
| 기울기 | → | 손목 |
| 엄지-검지 벌림(핀치) | → | 그리퍼 |

여기서 한 가지 함정이 있었다. 핀치 거리를 그냥 쓰면 **손을 멀리 뒀을 때 항상 '쥔 것'으로
읽힌다.** 손 전체가 작게 보이니까. 그래서 핀치를 **손 폭으로 나눠** 정규화했다.
가까이서 집든 멀리서 집든 같은 각도가 나온다.

### 카메라 없이 시험할 수 있게 쪼갰다

`hand_map.py`는 랜드마크 21점을 관절 5개로 바꾸는 **순수 계산**만 한다. 카메라도 팔도 모른다.
그래서 가짜 랜드마크를 만들어 시험할 수 있었고, 실제로 그렇게 했다 —
거리를 바꿔도 핀치 결과가 같은지, 극단값에서 안전범위를 넘지 않는지.

떨림 대책은 세 겹이다. 지수평활(잔떨림) → 불감대(3도 이하 무시) → 한 번에 최대 10도.
손은 떨리고 팔은 연약하다.

### 자잘하지만 실제로 겪은 것들

- **화면 글자를 영어로 바꿨다.** OpenCV 기본 글꼴은 한글을 그리지 못하고 `???`를 낸다.
- **전송을 조용히 하도록 따로 만들었다.** 브리지의 `send_serial`은 한 줄마다 화면에 찍는다.
  초당 십여 번 보내는 프로그램에서는 콘솔이 넘친다. 대신 아두이노의 `OK`는 기다린다 —
  그 기다림 자체가 자연스러운 속도 제한이 된다. 팔이 다 움직여야 다음이 간다.

---

## 10. 스마트 앱 제어 — 두 번 막히고, 한 번은 내가 틀렸다

윈도우 11의 '스마트 앱 제어'가 서명 없는 DLL을 막는다. 두 번 걸렸다.

### 1차: mediapipe

```
OSError: [WinError 4551] 애플리케이션 제어 정책에서 이 파일을 차단했습니다
```

버전별 구조를 뜯어보니 경계가 뚜렷했다.

| 버전 | 구조 | 결과 |
|---|---|---|
| 0.10.30 이상 | `libmediapipe.dll` 하나를 ctypes로 적재 | **차단** |
| 0.10.21 이하 | `.pyd` 확장모듈 방식 | 통과 |

그리고 **0.10.21에도 Tasks API가 그대로 있었다.** 코드를 고칠 필요 없이 버전만 낮춰 풀렸다.
휠 파일 안을 직접 열어 확인한 것이 결정적이었다 — 추측이 아니라 실측.

### 2차: torch

며칠 전에 그 torch로 모델을 구웠는데, 이번엔 `caffe2_nvrtc.dll`이 막혔다.
**그때는 되고 지금은 안 된다** — 평가 모드가 적용으로 넘어간 것으로 보인다.

이번엔 버전을 낮춰 피할 길이 없었다. 마이크로소프트가 명시하고 있다 —
**개별 파일 예외는 존재하지 않는다.** 전체를 켜거나 끄거나 둘 중 하나다.

### 내가 틀렸던 부분

나는 "한 번 끄면 윈도우를 다시 설치해야 다시 켤 수 있다"고 했다. 그건 옛 이야기였다.
**2025년 12월부터 24H2/25H2에서는 껐다 켤 수 있게 바뀌었다.** 내 지식이 그 시점에 멈춰 있었다.
확인하지 않고 말했다면 사용자는 되돌릴 수 있는 선택을 되돌릴 수 없는 것으로 알고 포기했을 것이다.

### 이 사건이 드러낸 것

torch가 막혔다는 것은 **`arm_tuned.py`, 우리가 직접 구운 모델도 그동안 못 쓰는 상태였다**는 뜻이다.
음성을 시도하지 않았다면 다음에 그 모델을 부를 때에야 알았을 것이다.

**교훈: 설치 환경도 실측 대상이다.** 그래서 `mp_check.py`를 만들었다 —
스마트 앱 제어 상태를 레지스트리에서 직접 읽고, 두 방식을 각각 실제로 돌려보고,
막혔는지 모델 파일만 없는지까지 구분한다. `pin_finder.ino`, `check_dataset.py`, `--diag`에 이어
네 번째 진단 도구다.

---

## 11. 목소리 — 새 DLL을 하나도 들이지 않기

손 추적이 도는 것을 확인하고 **몇 분 만에** 음성으로 넘어갔다. 이 사실 자체가 기록할 만하다.
카메라를 붙이는 데는 하루가 걸렸는데 목소리는 한나절 안에 끝났다. 새로 만든 것이 적어서다.

"텍스트 명령으로 움직이는 것을 확장해, 음성으로." 구조는 단순하다. **입구 하나만 바뀐다.**

```
(전)  키보드로 친 문장 ─┐
(신)  말 → Whisper ────┴→ 라마 → 검증·보정 → 아두이노
```

문장이 만들어진 뒤로는 지금까지의 길을 그대로 지난다. `enforce_duration`도 `home_policy`도
각도 제한도 전부 이미 있는 것을 쓴다. **음성이라고 팔의 규칙을 새로 만들지 않았다** —
규칙이 두 곳에 있으면 반드시 어긋난다는 것을 3번에서 이미 겪었으니까.

### 설계 제약: 새 바이너리 0개

스마트 앱 제어에 두 번 막힌 직후였다. 그래서 이렇게 정했다.

| 흔한 선택 | 문제 | 대신 쓴 것 |
|---|---|---|
| `sounddevice` · `pyaudio` | 서명 없는 portaudio DLL | **`winmm.dll`** (MCI) — 윈도우 기본, MS 서명 |
| 키 감지 라이브러리 | 〃 | **`user32.dll`** `GetAsyncKeyState` |
| `faster-whisper` | 서명 없는 CTranslate2 DLL | **`transformers` Whisper** — 라마 구울 때 쓴 torch 그대로 |

pip로 설치할 것이 하나도 없다. 새로 들어오는 바이너리가 없으니 막힐 것도 없다.

### 요청한 형식을 믿지 않았다

MCI에게 16kHz·모노·16비트로 녹음해 달라고 요청은 하되, **저장된 WAV의 머리말을 실제로 읽고**
필요하면 변환하도록 했다. 드라이버가 요청을 무시하는 일이 흔하기 때문이다.

그리고 실제로 그랬다. 첫 녹음이 **8비트**로 저장됐다.

그 대비를 만드는 과정에서 버그도 하나 잡혔다. **`audioop`은 8비트를 부호 있는 값(-128~127)으로
보는데, WAV의 8비트는 부호 없는 값(0~255)이다.** 이걸 빠뜨리면 소리가 통째로 망가지는데
에러는 나지 않는다. 7가지 형식 조합으로 대조해 잡았다.

### 왜 몇 분 만에 됐는가

음성에서 새로 만든 것은 **소리를 글자로 바꾸는 부분뿐이다.** 문장이 나온 뒤로는
0장에서 정한 역할 분담이 그대로 받아준다 — 라마가 뜻을 읽고, 코드가 시간·반복·한계를 지키고,
아두이노가 각도를 강제한다. 0장의 그림에서 맨 왼쪽 상자 하나만 갈아 끼운 셈이다.

카메라가 하루 걸린 것은 팔의 좌표와 화면의 좌표를 잇는 **새로운 대응**을 만들어야 했기 때문이고,
음성이 몇 분 걸린 것은 그런 것이 아예 없었기 때문이다. 처음에 정한 구조가
몇 주 뒤에 이런 식으로 값을 치러 준다.

### 안전 장치

- 스페이스를 **누르고 있는 동안만** 듣는다. 항상 듣고 있지 않다.
- 들린 문장을 화면에 먼저 띄우고, Enter를 눌러야 팔이 움직인다. `e`로 고쳐 쓸 수도 있다.
- Whisper가 무음에서 뱉는 자막 상투구("시청해주셔서 감사합니다" 등)를 걸러낸다.
  그대로 라마에 흘려보내면 팔이 엉뚱하게 움직인다.

---

## 12. 이사 — 이름을 외우지 않는 이사

작업공간을 `Downloads`에서 `D:\workspace_raraavis\arduino_project00`으로 옮겼다.

파일이 스무 개가 넘었다. 목록을 손으로 적으면 **반드시 하나는 빠뜨린다.**
그래서 이름 대신 **표시**로 찾게 했다.

- 학습된 모델 폴더 → `adapter_config.json`이 들어 있는 폴더
- 아두이노 스케치 → `.ino`가 들어 있는 폴더

이러면 내가 `arm-lora`라는 이름을 잘못 기억하고 있어도 딸려 온다.
실제로 시험할 때 `arm-lora-final`로 이름을 바꿔 두고 확인했다.

그리고 **복사만 하고 원본은 남겼다.** 복사 후 크기를 하나하나 대조하고,
원본에 남은 같은 종류 파일도 따로 보고한다 — 놓친 게 있으면 그 목록에 나타난다.

### PowerShell을 버리고 파이썬으로 다시 썼다

처음에 `.ps1`로 썼는데 실행 정책에 막혔다. 우회하는 한 줄을 알려줄 수도 있었지만
파이썬으로 다시 썼다. 이유는 하나다 — **PowerShell은 내가 시험해 볼 수 없다.**
파이썬이면 가짜 Downloads 폴더를 만들어(어댑터 이름을 바꾸고, `.ino`를 폴더 안팎에 흩고,
`.exe`·`.pdf`·`__pycache__`를 섞어) 실제로 돌려보고 줄 수 있다.

`checkup.py`는 새 자리가 온전한지 기능별로 점검한다. 무엇이 빠졌는지가 아니라
**빠진 것이 어느 기능을 못 쓰게 만드는지**를 말해준다.

---

## 배운 것

**1. 하나의 원칙이 거의 모든 문제를 풀었다.**
모델에게는 언어 판단만 맡기고, 시계·산수·반복·안전 한계·의도는 결정론적 코드가 맡는다. 시간 감각, 세기, 상대 각도, 그리고 오늘의 초→밀리초 환산과 복귀 의도까지 — 전부 이 원칙으로 해결됐다.

**2. 실물도, 데이터도, 장치도 "실측"해야 한다.**
배선은 실크 인쇄와 달랐고, 데이터는 의도와 달랐고, 카메라는 "없음"이 아니라 "점유 중"이었다. 세 번 다 추측을 멈추고 진단 도구를 만든 것(`pin_finder.ino`, `check_dataset.py`, `--diag`)이 전환점이었다.

**3. 규칙이 두 곳에 있으면 반드시 어긋난다.**
정제기와 브리지가 조금씩 다른 규칙을 갖고 있었던 것이 오늘 사고의 뿌리였다. 검사기를 만들어도 그 검사기가 다른 규칙을 쓰면 소용없다. 이제 `home_policy()` 하나가 유일한 정의다.

**4. 로그를 읽는 것이 전부다.**
"손목 대신 그리퍼가 움직인다", "계획은 7000인데 전송은 5000이다", "3번까지는 되는데 4번부터 안 된다", "10초인데 4000이다" — 이 관찰들이 매번 원인을 정확히 가리켰다. 로봇팔은 모델의 이상 행동을 몸으로 보여주는 **LLM 현미경**이었다.

**5. 개선은 느낌이 아니라 숫자로 확인한다.**
학습 전 점수를 먼저 재두지 않았다면 "좋아진 것 같은데?"라는 느낌만 남았을 것이다. 채점기가 매번 다른 점수를 내던 것(temperature 0.2)도 그래서 고쳐야 했다.

**6. 하드웨어가 못 하는 일은 소프트웨어로 못 채운다 — 목표를 낮추는 것도 설계다.**
평평한 티슈는 이 그리퍼로 집을 수 없고, 이 팔에는 카메라를 얹을 수 없다. 그 벽 앞에서
집기를 접고 가리키기로 내려온 것이 오늘의 결정적 판단이었다. 문제가 2차원에서 1차원으로,
검증 불가에서 단조성 검사 가능으로 바뀌었고, 그날 안에 동작하는 것이 나왔다.
무리해서 팔을 부러뜨리는 것보다 확실히 되는 것을 하나 만드는 편이 낫다.

**8. 인터넷 예제는 자기가 몇 년도 것인지 말해주지 않는다.**
`mp.solutions.hands`는 사라졌는데 예제의 열에 아홉이 아직 그 방식이다. 라이브러리를 처음 쓸 때
예제를 베끼기 전에 **설치된 판에 그 이름이 실제로 있는지** 확인하는 한 줄이 하루를 아낀다.

**9. 운영체제도 실측 대상이다 — 어제 되던 것이 오늘 안 될 수 있다.**
같은 컴퓨터, 같은 torch인데 며칠 사이에 막혔다. "환경은 그대로일 것"이라는 가정이
가장 조용히 배신한다. 그래서 진단기가 오류 메시지를 알아보고 다음 할 일을 말하게 했다.

**11. 좋은 구조는 몇 주 뒤에 값을 치러 준다.**
음성은 몇 분 만에 붙었다. 새로 만든 것이 "소리→글자" 하나뿐이었기 때문이다. 첫날 정한
역할 분담(모델은 언어, 코드는 산수와 안전)이 그때는 그저 신중함이었는데, 지금은
새 입구를 꽂을 자리가 이미 나 있다는 뜻이 됐다.

**10. 내가 시험할 수 없는 코드는 주지 않는다.**
PowerShell 스크립트는 검증 없이 건넨 것이었고, 실행 정책에서 바로 막혔다. 파이썬으로 다시 쓰니
가짜 폴더를 만들어 끝까지 돌려보고 줄 수 있었다. 시험 가능성은 언어 선택의 이유가 된다.

**7. 시스템이 모델의 실수를 덮어주는 것은 양날의 검이다.**
음수 각도 자동 해석, 시간 보정, home 보정 — 편리하지만 쌓이면 모델이 뭘 잘못하는지 보이지 않게 된다. 그래서 `--no-guard` 옵션으로 원출력을 볼 수 있게 남겨뒀다. 파인튜닝의 목적 중 하나는 이런 보정을 하나씩 걷어내는 것이다.

---

## 만든 것들

**하드웨어 제어**
- `robot_arm_llm.ino` — 시리얼 명령 수신 펌웨어 (D9/D6/D5/D3/D11, 그리퍼 60↔120도, 관절 한계 강제)
- `pin_finder.ino` — 핀-관절 매핑 진단 스케치
- `test_arm.py` — LLM 없는 하드웨어 검증 데모

**LLM 브리지**
- `llm_arm_bridge.py` — 명령 검증·전송의 단일 창구. `move`/`repeat`/`wait seconds`,
  `enforce_duration`(시간 보정), `home_policy`+`enforce_home_policy`(복귀·유지 의도 강제), 데이터 자동 기록
- `arm_tuned.py` — 학습된 모델 + 짧은 프롬프트로 팔 조종 (`--no-guard`로 원출력 확인 가능)

**데이터**
- `clean_dataset.py` → `fix_dataset_v2.py` — 1차·2차 정제
- `check_dataset.py` (v2) — 모순·부호·방향·home 정책·시간·대상·형식 검사
- `arm_dataset.jsonl`(131) → `arm_dataset_clean_fixed.jsonl`(98) → **`arm_dataset_v3.jsonl`**(98, 11곳 교정)

**학습**
- `arm_common.py` / `prepare_data.py` / `train_qlora.py` / `evaluate.py`
- `arm-lora/` — 학습된 어댑터

**시각 (신규)**
- `vision.py` — 색 기반 물체 검출, 카메라 열기·진단 (`diagnose`, `probe_variants`)
- `vision_check.py` — 색 범위 맞추기 + `--list` / `--diag` / `--probe`
- `calibrate_arm.py` — 집기 보정 (모순 검사·격자 커버리지·화면 저장). 하드웨어 한계로 보류
- **`calibrate_point.py`** — 가리키기 보정 (1차원, 단조성 검사, 직선 연장 추정)
- **`point_at.py`** — 물체를 보고 따라 도는 실행기 (불감대·중앙값·최소 간격)
- `가리키기_순서.md` — 단계별 실행 순서와 문제 해결표

**춤 (신규)**
- `dance.py` — 안무 8종(인사·웨이브·스윙·도리도리·집게박수·로봇춤·스윕·피날레),
  박자↔속도 역산, 춤 전용 안전 범위, `--amp` / `--no-grip` / `--bpm` / `--loop`
- 브리지의 `dance` 명령 — 모델은 안무 이름만 말하고 코드가 시퀀스로 펼친다

**손 추적 (신규)**
- `hand_map.py` — 랜드마크 21점 → 관절 5개 (순수 계산, 카메라·팔과 무관하므로 단독 시험 가능).
  핀치를 손 폭으로 정규화해 거리에 무관, `Smoother`로 평활·불감대·최대 이동량 3중 여과
- `hand_track.py` — 카메라·팔 연결. Tasks/solutions 두 API 자동 선택, 모델 자동 내려받기,
  일시정지·홈·좌우반전·집게 토글
- `mp_check.py` — mediapipe 진단. 스마트 앱 제어 상태 판독, 두 API 실제 구동, 차단과 모델 부재 구분

**음성 (신규)**
- `voice_io.py` — `winmm.dll` 녹음 + `user32.dll` 키 감지 + WAV 실측 변환 (새 바이너리 0개)
- `stt.py` — 이미 있는 torch/transformers로 Whisper. 자막 상투구 필터 포함
- `voice_check.py` — 마이크·GPU·받아쓰기 단계별 진단. 4551을 알아보고 다음 할 일을 안내
- `voice_arm.py` — 말 → 문장 → 기존 브리지. 확인 후 실행, `e`로 고쳐쓰기

**작업공간 (신규)**
- `move_project.py` — 이름이 아니라 표시로 찾는 이사 도구. 복사 후 크기 대조, 남은 파일 보고,
  학습 중간 저장본은 기본 제외
- `checkup.py` — 기능별 점검. 빠진 것이 어느 기능을 못 쓰게 만드는지까지

**문서**
- `가이드_라마_로봇팔.md` / `README_학습.md` / 이 기록

---

## 지금 상태와 다음

**되는 것**
- 자연어로 팔 조종 — 학습된 전용 모델, 시스템 프롬프트에 규칙·예시 없이
- 시간·복귀 의도 자동 보정 (`enforce_duration` / `enforce_home_policy`)
- **카메라로 물체를 보고 팔이 그쪽을 향해 돌아본다** (보정 5점, 오차 1.9도 이내)
- **자연어로 춤** — "인사하고 나서, 30초 동안 로봇춤을 추시오"까지 한 문장으로
- **손을 보고 따라 움직인다** — 사람 손 하나를 인식해 5축에 실시간 대응
- **말로 조종한다** — 스페이스를 누르고 말하면 받아쓴 문장이 그대로 기존 파이프라인으로

**다음 후보**
- 보정점을 화면 왼쪽에 1~2개 추가 (현재 보정 구간은 화면 오른쪽 31%, 나머지는 직선 연장 추정)
- 재학습 (`arm_dataset_v3.jsonl` + `SHORT_SYSTEM`에 `wait seconds`) → 브리지 보정을 하나씩 걷어내기
- 시각과 언어의 결합: "보이는 것 쪽을 봐" 를 자연어로
- 학습 모델에도 춤 붙이기 — `SHORT_SYSTEM`에 dance 추가 + 춤 예시를 데이터에 넣고 재학습
- 집기: 그리퍼 폭에 맞는 세울 수 있는 물체를 먼저 정하고 재시도
- `arm_dataset_v2.jsonl`(2차 기록)이 쌓이면 검사 후 합치기
- 손 추적과 음성을 하나로: "따라 해" 라고 말하면 손 추적이 켜지게
- `hand_map.py`의 감도(`SPAN_NEAR/FAR`, `PINCH_*`) 실사용에 맞춰 조정
- 아두이노 스케치 정리 — `robot_arm_llm` / `_1` / `_2` 중 현재 것만 남기기

---

*첫째 날에는 나사를 조였고, 둘째 날에는 전용 언어모델을 구웠다.*
*며칠 뒤 이 작은 나무 팔은 책상 위의 물건을 보고 고개를 돌렸고,*
*말 한마디에 인사를 하고 춤을 췄다.*

*그리고 이제, 사람의 손을 따라 움직이고 목소리를 알아듣는다.*

*돌아보면 고비마다 답은 같았다 — 추측을 멈추고, 재는 도구를 만드는 것.*
*`pin_finder.ino`, `check_dataset.py`, `--diag`, `mp_check.py`, `voice_check.py`, `checkup.py`.*
*여섯 개의 진단 도구가 이 기록의 진짜 줄거리다.*
