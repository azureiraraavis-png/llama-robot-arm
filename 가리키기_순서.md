# 가리키기 — 실행 순서

**목표**: 책상에 놓인 티슈를 카메라가 보고, 팔이 그쪽을 향해 몸을 돌린다.
**필요한 것**: 지금 배치 그대로 (모니터 앞 웹캠, 오른쪽의 팔, 키보드 앞 빈 공간)
**팔이 물체에 닿을 필요는 없습니다.** 제자리에서 방향만 돌립니다.

---

## 0. 시작 전 (중요)

장치는 한 프로그램만 잡을 수 있습니다. 아래를 **모두 닫으세요.**

- 윈도우 **카메라 앱** (지난번 "읽기 실패"의 원인이었습니다)
- Arduino IDE의 **시리얼 모니터**
- 이전에 띄워둔 **브리지 / 보정 창**

그리고 **키보드 앞 공간을 비웁니다.** 마우스는 옆으로 밀어두고, 티슈를 놓을 자리 세 곳
(왼쪽 · 가운데 · 오른쪽)을 확보하세요. 팔 쪽으로 너무 붙일 필요 없습니다.

---

## 1. 보정 (한 번만)

```
py -3.12 calibrate_point.py --fresh
```

### 1-1. 가리키는 자세 만들기

창이 뜨면 하단에 `[1단계]`가 보입니다. 팔이 **앞으로 뻗어 손가락질하는 모양**이 되게 만드세요.

| 키 | 관절 |
|---|---|
| `w` / `s` | 어깨 (위/아래) |
| `q` / `e` | 팔꿈치 |
| `z` / `c` | 손목 |
| `[` / `]` | 조작 단위 조절 (기본 5도) |

옆에서 봤을 때 **어느 쪽을 향하는지 한눈에 보이는 자세**면 됩니다.
지금처럼 위로 곧게 선 자세는 방향을 알기 어려우니, 앞으로 눕히세요.

자세가 잡히면 **`t`** — 이 자세는 이후 고정되고, 베이스만 돌게 됩니다.

### 1-2. 세 자리에서 기록

하단이 `[2단계]`로 바뀝니다. 한 자리마다 이렇게 합니다.

1. **티슈를 놓는다** (첫 자리: 키보드 앞 **왼쪽**, 마우스 있던 쪽)
2. **손을 화면에서 치운다** → 초록 박스가 티슈를 감싸는지 확인
3. **`r`** — 화면에 보라색 세로선이 그어지며 위치가 기억됨
4. **`a` / `d`** 로 베이스를 돌려 **팔이 티슈를 가리키게** 함
   - 이때 팔이 티슈를 가려도 괜찮습니다. 위치는 이미 기억됐습니다.
   - 티슈를 건드리지 않도록만 주의하세요.
5. **스페이스** — `✔ 1점: x=... → 베이스 ...도` 가 뜨면 성공

**티슈를 가운데로 옮겨** 2~5를 반복, **오른쪽으로 옮겨** 한 번 더.
**세 자리면 충분합니다.** 여유가 되면 4~5곳이 더 좋습니다.

### 1-3. 저장

**`x`** 를 누르면 저장되고 요약이 나옵니다.

```
3점 저장 → ...\point_calib.json
── 품질 요약 ────
  가로 커버리지 180~470 (화면의 45%)
  단조 관계 ✓ (감소)
  직선 기울기 -0.221 도/픽셀 (화면 전체 640px → 약 141도 회전)
  추정: 화면 왼쪽끝 161도 · 중앙 90도 · 오른쪽끝 20도
```

**`단조 관계 ✓`가 보이면 성공입니다.** 마지막 줄의 추정값이 상식적인지도 봐주세요
(왼쪽에 있을 때 각도가 크고 오른쪽에서 작거나, 그 반대면 정상).

---

## 2. 실행

```
py -3.12 point_at.py
```

티슈를 책상 위에서 **좌우로 천천히 움직여 보세요.** 팔이 따라옵니다.

- 화면에 `x=320 → 베이스 90도` 처럼 현재 판단이 표시됩니다
- 보정 범위 밖이면 주황색으로 `(보정 범위 밖 — 끝값 사용)` 이 뜨지만, 직선으로 이어서 추정하므로 동작은 합니다
- **`q`** 로 종료 (팔은 홈으로 돌아갑니다)

한 번만 가리키게 하려면 `--once`, 팔 없이 예측만 보려면 `--dry-run`.

---

## 잘 안 될 때

| 증상 | 대처 |
|---|---|
| `⚠ 물체가 안 보입니다` | 손이나 팔이 티슈를 가리고 있음. 치우고 다시 `r` |
| `⚠ 기존 기록과 가로로 너무 가깝습니다` | 티슈를 좌우로 더 멀리 옮기세요 (45픽셀 이상) |
| `⚠ 단조성 깨짐` | 조준이 어긋났습니다. 정말 티슈를 향하는지 옆에서 확인하고 다시 |
| 카메라가 안 열림 | 카메라 앱·화상회의 앱 종료 확인 |
| 팔이 안 움직임 | 다른 창이 COM4를 잡고 있음. 전부 닫고 재실행 |
| 팔이 부들부들 떨림 | `point_at.py --deadband 5 --interval 0.5` 로 둔감하게 |
| 티슈가 잘 안 잡힘 | `py -3.12 vision_check.py` 로 색을 다시 맞추고 `s` 저장 |

---

## 참고

- 보정은 **카메라와 팔이 움직이지 않는 한** 계속 유효합니다. 둘 중 하나라도 옮기면 다시 하세요.
- 보정을 이어서 더 하려면 `--fresh` 없이 실행하면 기존 점에 추가됩니다.
- 팔이 연약하니 무리한 자세는 피하고, 서보에서 "지잉" 소리가 나면 즉시 `h`(홈)를 누르세요.
